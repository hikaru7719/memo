import { Layout } from "antd";

export const Header = () => {
  const { Header } = Layout;
  return (
    <Header style={{ background: "#fff" }}>
      <p style={{ fontSize: "15px" }}>Memo</p>
    </Header>
  );
};
